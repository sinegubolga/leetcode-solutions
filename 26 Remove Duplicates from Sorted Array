func removeDuplicates(nums []int) int {
    k:=0
    m:=make(map[int]int)
    for i:=0;i<len(nums);i++ {
        if m[nums[i]]==0 {
            m[nums[i]]++
            nums[k]=nums[i]
            k++
        }
    }
    return k
    
}
